---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div>
  <strong>Tags:&ensp;</strong>
  {
    tags.map((t) => (
      <>
        <code>{t}</code>&ensp;
      </>
    ))
  }
</div>
<br />

<style>
  code {
    color: var(--contentColor);
  }
</style>
